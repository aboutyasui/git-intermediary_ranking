<% if client_signed_in? %>
  <div class="search_form">
    <%= form_tag(search_path, method: :post) do %>
      <%= text_field_tag :word %>
      <%= select_tag(:range, options_for_select([['Trader'], ['Post']])) %>
      <%= select_tag(:search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"],  ["後方一致","backward_match"], ["部分一致","partial_match"]])) %>
      <%= submit_tag "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% elsif trader_signed_in? %>
  <div class="search_form">
    <%= form_tag(search_path, method: :post) do %>
      <%= f.text_field :word %>
      <%= f.select :range, options_for_select([['Client'], ['Review']]) %>
      <%= f.select :search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"],  ["後方一致","backward_match"], ["部分一致","partial_match"]]) %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% elsif admin_signed_in? %>
  <div class="search_form">
    <%= form_tag(search_path, method: :post) do %>
      <%= f.text_field :word %>
      <%= f.select :range, options_for_select([['Trader'], ['Client'], ['Post'], ['Review']]) %>
      <%= f.select :search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"],  ["後方一致","backward_match"], ["部分一致","partial_match"]]) %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>
